name: Auto-merge claude branches to main

on:
  push:
    branches:
      - 'claude/*'

jobs:
  merge:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      administration: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Disable GitHub Pages before merge (prevent auto-deploy)
        run: |
          gh api -X DELETE /repos/${{ github.repository }}/pages 2>&1 || echo "Pages already disabled or error (ok)"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Merge to main
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git checkout main
          git pull origin main
          git merge --no-ff origin/${{ github.ref_name }} -m "Auto-merge ${{ github.ref_name }} into main"
          git push origin main

      - name: Disable GitHub Pages after merge (belt and suspenders)
        run: |
          gh api -X DELETE /repos/${{ github.repository }}/pages 2>&1 || echo "Pages already disabled or error (ok)"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
