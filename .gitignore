# dependencies
node_modules/

# build output — CF Pages rebuilds this itself (npm run build) on every push
dist/

# credentials & browser session — never commit
.game-creds.json
tools/.session.json

# generated data exports (live game-state snapshots)
exports/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
